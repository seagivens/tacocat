<h2> Add a task </h2>

<%= form_with model: @task, local: true do  |f| %>

<% if @task.errors.any? %>
<h5>
  <%= pluralize(@article.errors.count, "error") %> prohibited this task from being saved: </h5>

  <ul>
    <% @task.errors.full_messages.each do |msg| %>
    <li><%= msg %></li>
    <% end %>
  </ul>
</div>
<% end %>

<p>
  <%= f.label :title %> <br>
  <%= f.text_field :title %>
</p>

<p>
  <%= f.label :description %><br>
  <%= f.text_area :description %>
</p>
<p>
<%= f.submit %>
  <% end %>
